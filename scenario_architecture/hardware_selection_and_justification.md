# Component B — Hardware Selection and Roofline Analysis

This content has moved to `hardware/hardware_justification.md`, which is
the canonical location per the assignment's suggested repository
structure (Deliverable 1). This file previously duplicated that content
with a few numbers that had drifted out of sync (an unverified "<15ms"
Pi-specific latency claim never actually measured on real Pi hardware,
and an inconsistent "<10s detection" SLA label) -- consolidated to avoid
two files disagreeing with each other in the viva.

See `hardware/hardware_justification.md` for Task B1 (Constraint
Triangle, hardware selection argument, 85-truck pilot / 265-truck fleet
cost) and Task B2 (Arithmetic Intensity and Roofline analysis).

The Constraint Triangle diagram (`constraint_triangle.svg`) remains in
this directory and is referenced from the canonical file above.
